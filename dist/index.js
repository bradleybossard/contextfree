var compiler = require('compiler');

function compile(cfdg) {
  var compiler = Compiler();
  return true;
}

function render(compiled) {
  return true;
}

module.exports = {
  compile: compile,
  render: render
};
